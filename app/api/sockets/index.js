// Main file for socket.io listeners

/**
 * Function to listen on 'connection'
 * @param options {Object} - Options
 */
module.exports = (options) => {
  return (socket) => {
    console.log(this);
  };
};
