// Index of models
// Require & export
const App = require('./apps');
const Notification = require('./Notifications');
const User = require('./User');

module.exports = {
  App,
  Notification,
  User
};
