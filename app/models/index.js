// Index of models
// Require & export
const Notification = require('./Notifications');
const Task = require('./Tasks');
const User = require('./User');

module.exports = {
  Notification,
  Task,
  User
};
